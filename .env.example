# Anthropic API — used by the order planner (and by agent engines in stage 4).
# Without a key the app still works: the planner falls back to a simple
# keyword-based heuristic.
ANTHROPIC_API_KEY=

# Optional: model used for planning. Defaults to claude-opus-4-8.
# Cheaper option: claude-sonnet-5
ANTHROPIC_MODEL=
